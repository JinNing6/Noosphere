name: Publish noosphere-mcp to PyPI

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  build:
    name: Build and verify package
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install package and dev quality gates
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Run SDK release tests
        run: python -m pytest tests

      - name: Run shared Skill supply-chain tests
        working-directory: .
        run: |
          node --test .github/scripts/*.test.cjs
          python -m unittest scripts.test_validate_shared_skills scripts.test_migrate_consciousness_promotions scripts.test_launch_surface scripts.test_check_validation_result
          python scripts/validate_shared_skills.py
          python scripts/migrate_consciousness_promotions.py --check

      - name: Run PyPI verifier tests
        working-directory: .
        run: python -m unittest scripts.test_verify_pypi_release

      - name: Verify MCP server import
        run: python -c "import noosphere.noosphere_mcp as server; assert server.mcp is not None"

      - name: Build sdist and wheel
        run: python -m build

      - name: Upload package distributions
        uses: actions/upload-artifact@v4
        with:
          name: noosphere-mcp-dist
          path: sdk/dist/

  publish-pypi:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/noosphere-mcp/
    permissions:
      id-token: write

    steps:
      - name: Download package distributions
        uses: actions/download-artifact@v4
        with:
          name: noosphere-mcp-dist
          path: dist/

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  verify-pypi:
    name: Verify PyPI install
    needs: publish-pypi
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Verify published PyPI package with server/discover + tools/list and initialize + tools/list
        run: python scripts/verify_pypi_release.py --tool-count 46

  refresh-pages-proof:
    name: Dispatch Pages traction proof refresh
    needs: verify-pypi
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write

    steps:
      - name: Dispatch Pages deploy on main after PyPI verification
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          curl -fsSL -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GITHUB_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/deploy-pages.yml/dispatches" \
            -d '{"ref":"main"}'
